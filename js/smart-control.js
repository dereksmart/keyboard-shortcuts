jQuery(document).ready(function($){
    /*
    var pressedKeys = {};
    // The keyboard shortcut is cmd + shift + o + s
    var acceptedKeys = [ 16 ];
    var keyCombo = {};

    $(document).keydown(function (event) {
        if ( acceptedKeys.indexOf( event.which ) > -1 ) {
            pressedKeys[event.which] = true;

            bundleKeys();
        }

        console.log( keyCombo );
    });

    function bundleKeys() {
        for ( var i in pressedKeys ) {
            if ( !pressedKeys.hasOwnProperty(i) ) continue;
            keyCombo.push(i);
        }

        // Listens for a combo of cmd + shift + o + s
        if ( keyCombo === '161616' ) {
            alert( 'three times' );
        }
    }
    */
});